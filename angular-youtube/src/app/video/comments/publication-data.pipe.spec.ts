import { PublicationDataPipe } from './publication-data.pipe';

describe('PublicationDataPipe', () => {
  it('create an instance', () => {
    const pipe = new PublicationDataPipe();
    expect(pipe).toBeTruthy();
  });
});
